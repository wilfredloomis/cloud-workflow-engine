{
  "name": "cloud-build-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for Cloud APK Build Engine",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

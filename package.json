{
  "name": "cloud-workflow-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud APK Build Engine - Cloudflare Worker backend",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
